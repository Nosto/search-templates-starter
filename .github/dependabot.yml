version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"
    open-pull-requests-limit: 10
    groups:
      storybook:
        patterns:
          - "@storybook/*"
          - "storybook"
      eslint:
        patterns:
          - "eslint"
          - "eslint-*"
          - "@types/eslint-*"
          - "prettier"
      semantic-release:
        patterns:
          - "@semantic-release/*"
          - "semantic-release"
      vitest:
        patterns:
          - "@playwright/test"
          - "vitest"
      stylelint:
        patterns:
          - "stylelint"
          - "stylelint-*"